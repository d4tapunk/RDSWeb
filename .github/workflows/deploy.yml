name: Deploy GitHub Pages

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the code from the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Install dependencies (optional step, only needed if you have a build process)
      # Uncomment this if you have a build process with Node.js or another tool
      # - name: Install dependencies
      #   run: npm install

      # Step 3: Build the project (optional, only if you have a build process)
      # Uncomment this if your frontend needs to be built (e.g., React, Vue, etc.)
      # - name: Build project
      #   run: npm run build

      # Step 4: Deploy the frontend folder to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend  # This is the path to your frontend folder
